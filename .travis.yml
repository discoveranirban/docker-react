sudo: required
services:
  - docker

before_install:
  - docker build -t discoveranirban/docker-react -f Dockerfile.dev .

script:
  - docker run discoveranirban/docker-react npm run test -- --coverage